import 'package:get/get.dart';class PlaybackItemModel {Rx<String> typeTxt = Rx("Automix");

Rx<String> descriptionTxt = Rx("Allows seamless transitions between songs on select playlists.");

Rx<bool> isSelectedSwitch = Rx(false);

Rx<String>? id = Rx("");

 }
